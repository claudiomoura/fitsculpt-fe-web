{
  "name": "fitsculpt-release-gates",
  "private": true,
  "scripts": {
    "release:check": "npm run release:check:fe && npm run release:check:be",
    "release:check:fe": "npm --prefix apps/web run build && npm --prefix apps/web run typecheck && npm --prefix apps/web run test",
    "release:check:be": "npm --prefix apps/api run lint && npm --prefix apps/api run typecheck && npm --prefix apps/api run build && npm --prefix apps/api run test",
    "rc:gate:bundle": "npm run release:check && npm --prefix apps/web run test:contracts",
    "ci:build": "npm --prefix apps/web run build && npm --prefix apps/api run build",
    "ci:typecheck": "npm --prefix apps/web run typecheck && npm --prefix apps/api run typecheck",
    "ci:test": "npm --prefix apps/web run test && npm --prefix apps/api run test",
    "ci:e2e:smoke": "npm --prefix apps/web run e2e -- e2e/library-smoke.spec.ts",
    "seed:demo": "npm --prefix apps/api run seed:demo",
"setup:linear": "node scripts/setup-linear-core.mjs",
    "setup:linear:check": "node scripts/setup-linear-core.mjs --check",
    "setup:linear:workflow-cycles": "node scripts/setup-linear-workflow-cycles.mjs",
    "setup:linear:workflow-cycles:check": "node scripts/setup-linear-workflow-cycles.mjs --check",
    "setup:linear:issues": "node scripts/setup-linear-issues.mjs",
    "setup:linear:issues:check": "node scripts/setup-linear-issues.mjs --check",
    "setup:notion-hq": "node scripts/setup-notion-hq.mjs",
    "setup:notion-hq:check": "node scripts/setup-notion-hq.mjs --check",
    "sync:notion-hq": "node scripts/sync-notion-hq.mjs",
    "sync:notion-hq:check": "node scripts/sync-notion-hq.mjs --check",
    "android:prepare": "npm --prefix apps/web run mobile:android:prepare",
    "android:sync": "npm --prefix apps/web run mobile:android:sync",
    "android:doctor": "npm --prefix apps/web run mobile:android:doctor"
  }
}
